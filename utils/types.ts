export type TeamData = {
    id: string;
    name: string;
    timeslot: string;
    leader: string;
    location?: string;
    region?: string;
}

export type Roles = {
    callsign: string;
    role: string;
    color: string;
}